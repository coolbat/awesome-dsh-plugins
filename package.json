{
  "name": "awesome-dsh-plugins",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "An evidence-led, bilingual directory of DeepSeek Harness plugins.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "npx serve out",
    "generate": "node scripts/generate-readmes.mjs",
    "discover": "node scripts/discover.mjs",
    "validate:candidates": "node scripts/validate-candidates.mjs",
    "validate:review": "node scripts/validate-review-ledger.mjs",
    "validate": "node scripts/validate.mjs && npm run validate:candidates && npm run validate:review",
    "docs:check": "node scripts/generate-readmes.mjs --check",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/**/*.test.ts",
    "lint": "prettier --check src tests scripts/discover.mjs scripts/validate-candidates.mjs scripts/validate-review-ledger.mjs scripts/lib/discovery.mjs scripts/lib/discovery.d.mts scripts/lib/review-ledger.mjs scripts/lib/review-ledger.d.mts config/discovery.json data/candidates.json data/discovery-state.json data/review-*.json data/tombstones.json reports/discovery/latest.json .github/workflows next.config.ts tsconfig.json package.json",
    "format": "prettier --write src tests scripts/discover.mjs scripts/validate-candidates.mjs scripts/validate-review-ledger.mjs scripts/lib/discovery.mjs scripts/lib/discovery.d.mts scripts/lib/review-ledger.mjs scripts/lib/review-ledger.d.mts config/discovery.json data/candidates.json data/discovery-state.json data/review-*.json data/tombstones.json reports/discovery/latest.json .github/workflows next.config.ts tsconfig.json package.json",
    "check": "npm run validate && npm run docs:check && npm run typecheck && npm run test && npm run lint"
  },
  "dependencies": {
    "next": "^16.2.11",
    "react": "^19.2.1",
    "react-dom": "^19.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "prettier": "^3.8.4",
    "serve": "^14.2.5",
    "tsx": "^4.21.0",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
